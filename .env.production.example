# Production Environment Configuration Template
# Copy this file to .env.production and fill in your actual production values
# NEVER commit .env.production to version control

# Node Environment (should be production)
NODE_ENV=production

# MongoDB Connection String
# Format: mongodb+srv://username:password@cluster.mongodb.net/database
# For production, ensure:
# - Strong password
# - IP whitelist configured in MongoDB Atlas
# - Database user has appropriate permissions
MONGODB_URI=mongodb+srv://your-production-username:your-production-password@your-production-cluster.mongodb.net/your-production-database

# JWT Secret Key for Token Signing
# Generate a strong random secret using:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# CRITICAL: Use a different secret than development
JWT_SECRET=your-super-secure-production-jwt-secret-minimum-32-characters

# Allowed CORS Origins (comma-separated)
# List all domains that should access your API
# Examples:
# - For Linode/K8s: https://yourdomain.com,https://www.yourdomain.com
# - For testing: https://staging.yourdomain.com
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Optional: Application Port (if different from default)
# PORT=8000

# Optional: Additional security configurations
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW_MS=900000
